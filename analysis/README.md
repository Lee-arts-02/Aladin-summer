# Analysis Scripts

This folder is reserved for reproducible scripts.

Current analyses were run interactively in Codex and the outputs are documented in:

- `docs/analysis_log.md`
- `docs/methods_codebook.md`
- `docs/results_summary.md`

Recommended next step:

Convert the interactive analyses into standalone scripts:

1. `01_rq1_learning_gains.py`
2. `02_rq2_profiles_no_colonial.py`
3. `03_rq3_agency.py`
4. `04_ena_analysis.py`

Each script should read from `data/`, write to `outputs/`, and update or verify the result files listed in `docs/analysis_log.md`.

